DATABASE_URL="postgresql://postgres:postgres@localhost:5432/crewgoals"
AUTH_SECRET="replace-with-at-least-32-random-characters"
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# Optional: real transactional email. Without RESEND_API_KEY, messages are
# recorded in EmailLog and printed to the console (dev-friendly).
RESEND_API_KEY=""
EMAIL_FROM="CrewGoals <onboarding@resend.dev>"
